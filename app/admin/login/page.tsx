'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const r=useRouter();const [pw,setPw]=useState('');return <main className="login-page ocean-page"><div className="login-box"><h1 className="pixel">REMO Admin</h1><input defaultValue="remo_admin"/><input type="password" placeholder="비밀번호" value={pw} onChange={e=>setPw(e.target.value)}/><button className="primary" onClick={()=>r.push('/admin')}>로그인</button></div></main>}
